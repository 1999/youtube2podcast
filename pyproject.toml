[project]
name = "youtube2podcast"
version = "0.1.0"
description = "Convert YouTube channels into podcast feeds"
requires-python = ">=3.11"
dependencies = [
    "yt-dlp>=2024.1.0",
    "boto3>=1.34.0",
    "feedgen>=0.9.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]
